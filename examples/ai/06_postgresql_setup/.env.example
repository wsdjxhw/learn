# 这个模块默认先用 SQLite，保证你不安装 PostgreSQL 也能启动服务。
# 跑通默认流程后，再把下面这一行改成 PostgreSQL 连接串。
DATABASE_URL=sqlite:///./postgresql_setup.db

# PostgreSQL 示例：
# 1. ai_learn 是数据库名，需要先在 PostgreSQL 里创建。
# 2. postgres:postgres 分别是用户名和密码，请改成你自己的。
# 3. postgresql+psycopg 表示 SQLAlchemy 使用 psycopg 这个 PostgreSQL 驱动。
# DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/ai_learn
