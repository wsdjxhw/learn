# 默认仍使用 SQLite，保证不安装 PostgreSQL 也能学习 Alembic 的基本流程。
DATABASE_URL=sqlite:///./alembic_migrations.db

# 如果你已经完成上一节 PostgreSQL，可以改成真实 PostgreSQL。
# 建议使用一个新的数据库，避免和 06_postgresql_setup 的练习数据混在一起。
# DATABASE_URL=postgresql+psycopg://postgres:your-password@localhost:5432/ai_learn_migrations
